name: ac_chat_core
description: Pure-Dart core models, API contracts, and interfaces for the ac_chat ecosystem. No Flutter dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  autocode:
    path: './../autocode'
  ac_mirrors:
    path: './../ac_mirrors'
  ac_extensions:
    path: './../ac_extensions'

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
