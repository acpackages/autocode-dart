name: autocode_web_on_jaguar
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  autocode:
    path: './../autocode'
  autocode_data_dictionary:
    path: './../autocode-data-dictionary'
  autocode_extensions:
    path: './../autocode-extensions'
  autocode_sql:
    path: './../autocode-sql'
  autocode_web:
    path: './../autocode-web'
  jaguar: ^3.1.4
  jaguar_cors: ^3.0.2
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

dependency_overrides:
  mime: ^2.0.0
